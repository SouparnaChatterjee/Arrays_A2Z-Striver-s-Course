#include <bits/stdc++.h> 
bool checkArr(int n, vector<int> &arr){
    // Write your code here.
    if(n==0 || n==1)
    return true;
    for(int i=0;i<n;i++)
    {
        if(arr[i+1]<arr[i])
        return false;
    }
